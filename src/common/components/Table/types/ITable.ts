
// import type { ColumnFiltersState, PaginationState, SortingState } from '@tanstack/react-table'

export interface ITableParams {
  size: number;
  currentPage: number;
  globalFilter?: string;
  id_tpo_object?: number;
}